// Toggle this back to true once sign-in (word book / quiz / history) is
// confirmed working end-to-end. While false, those nav links and save
// buttons show as disabled instead of leading to a half-tested flow.
export const AUTH_ENABLED = true;